<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 p-8 text-white">
        <div class=" w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 text-sm mb-1 inline-block" href="/logout">Logout</a>
        </div>
        <h5 class="mb-5">Edit your Post.</h5>
        <form action="/update/<%=post._id%>" method="post">
            <textarea placeholder="what's in your mind?" class="p-3 outline-none w-1/3 resize-none bg-transparent block border-2 border-zinc-800 rounded-md" name="content"><%=post.content%></textarea>
            <input class="px-3 py-2 mt-2 w-40 rounded-md text-black bg-yellow-500 block" type="submit" value="Update Post">
        </form>
    </div>
</body>
</html>